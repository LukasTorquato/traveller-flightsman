__pycache__/
*.pyc
.venv/
.env
output/*.json
!output/.gitkeep
dist/
build/
*.egg-info/
.pytest_cache/
.ruff_cache/

# Runtime scan data lives in the private `runtime/` submodule (traveller-runtime repo).
# Nothing to ignore here — the submodule is its own repo with its own .gitignore.

# Claude Code local settings and session state
.claude/settings.local.json
.planning/

# IDE
.vscode/
.idea/

# Windows
Thumbs.db
desktop.ini

# Type checkers
.mypy_cache/
.pyright/
pyrightconfig.json

# Coverage
.coverage
htmlcov/
